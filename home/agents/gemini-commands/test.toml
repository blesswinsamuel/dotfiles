description = "Run tests and report failures with fix suggestions"

prompt = """
Run the project's test suite. Check package.json scripts, Makefile, Taskfile, or README to find the right test command. Report any failures with the file, test name, error message, and a suggested fix. If all tests pass, confirm with the count.
"""
