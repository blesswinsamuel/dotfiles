---
description: Run tests and report failures with fix suggestions
---

Run the project's test suite (check package.json, Makefile, Taskfile, or README to find the right command). Report any failures with the file, test name, error message, and a suggested fix. If all tests pass, confirm with the count.
