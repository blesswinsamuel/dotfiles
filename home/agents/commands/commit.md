---
description: Stage changes and create a conventional commit
---

Review staged and unstaged changes with `git status` and `git diff`, then stage all relevant files and commit with a conventional commit message (`feat:`, `fix:`, `chore:`, `refactor:`, `docs:`, `test:`). Keep the subject under 72 chars. Show the result with `git log --oneline -1`.
