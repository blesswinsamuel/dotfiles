function fish_greeting
    # set -l hour (date +%H) # Hour of the day

    # if test "$hour" -lt 12
    #     echo "Good Morning!"
    # else if test "$hour" -lt 16
    #     echo "Good Afternoon!"
    # else
    #     echo "Good Evening!"
    # end
end
